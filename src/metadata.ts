//  ========
//  METADATA
//  ========

/** Metadata inputs */
export const inputs = {
    src: 'src',
    dest: 'dest',
    slots: 'slots',
    removeSlots: 'remove-slots',
    isDryRun: 'dry-run'
} as const

/** Metadata outputs */
export const outputs = {
    contents: 'contents'
} as const